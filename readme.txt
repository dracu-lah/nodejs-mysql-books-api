npm init 
npm install express mysql 
npm install nodemon --save-dev (for adding as a development dependency)
npm start
npm install cors
npm install body-parser
npm install jsonwebtoken (used to change to data to token)